'use client'

/**
 * @deprecated Auth is handled by the global `AuthModal`. Kept as a no-op export
 * so older imports still compile.
 */
export function GlobalAuthLoginFallback() {
  return null
}

export default GlobalAuthLoginFallback
