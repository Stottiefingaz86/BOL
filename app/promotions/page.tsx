'use client'

/**
 * `/promotions` renders the casino shell in Promotions mode.
 * Re-export (no redirect) so the address bar never flashes `/casino?vipRewardsPage=true`.
 */
export { default } from '../casino/page'
